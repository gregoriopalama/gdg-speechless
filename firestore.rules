rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /sessions/{session} {
      allow read, write: if true; // Inizialmente aperto per la configurazione locale/demo
    }
  }
}
